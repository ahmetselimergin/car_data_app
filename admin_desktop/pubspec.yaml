name: admin_desktop
description: Cardex Admin — macOS masaüstü yönetim paneli
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  file_picker: ^8.3.7
  flutter_dotenv: ^5.2.1
  go_router: ^15.1.2
  path: ^1.9.1
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.3
  shadcn_flutter: ^0.0.52
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
